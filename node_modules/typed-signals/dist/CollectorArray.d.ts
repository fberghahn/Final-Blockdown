import { Collector } from "./Collector";
/**
 * Returns the result of the all signal handlers from a signal emission in an array.
 *
 * @typeparam THandler The function signature to be implemented by handlers.
 */
export declare class CollectorArray<THandler extends (...args: any[]) => any> extends Collector<THandler> {
    private result;
    handleResult(result: ReturnType<THandler>): boolean;
    /**
     * Get the list of results from the signal handlers.
     */
    getResult(): Array<ReturnType<THandler>>;
    /**
     * Reset the result
     */
    reset(): void;
}
