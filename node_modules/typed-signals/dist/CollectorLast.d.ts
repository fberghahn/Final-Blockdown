import { Collector } from "./Collector";
/**
 * Returns the result of the last signal handler from a signal emission.
 *
 * @typeparam THandler The function signature to be implemented by handlers.
 */
export declare class CollectorLast<THandler extends (...args: any[]) => any> extends Collector<THandler> {
    private result;
    handleResult(result: ReturnType<THandler>): boolean;
    /**
     * Get the result of the last signal handler.
     */
    getResult(): ReturnType<THandler> | undefined;
    /**
     * Reset the result
     */
    reset(): void;
}
