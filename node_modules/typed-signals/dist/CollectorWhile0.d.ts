import { Collector } from "./Collector";
/**
 * Keep signal emissions going while all handlers return false.
 *
 * @typeparam THandler The function signature to be implemented by handlers.
 */
export declare class CollectorWhile0<THandler extends (...args: any[]) => boolean> extends Collector<THandler> {
    private result;
    handleResult(result: boolean): boolean;
    /**
     * Get the result of the last signal handler.
     */
    getResult(): boolean;
    /**
     * Reset the result
     */
    reset(): void;
}
