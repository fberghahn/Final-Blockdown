/**
 * SignalLink implements a doubly-linked ring with nodes containing the signal handlers.
 * @private
 */
export declare class SignalLink<THandler extends (...args: any[]) => any> {
    private enabled;
    private readonly order;
    newLink: boolean;
    next: SignalLink<THandler>;
    prev: SignalLink<THandler>;
    callback: THandler | null;
    constructor(prev?: SignalLink<THandler> | null, next?: SignalLink<THandler> | null, order?: number);
    isEnabled(): boolean;
    setEnabled(flag: boolean): void;
    unlink(): void;
    insert(callback: THandler, order: number): SignalLink<THandler>;
}
